# G5 — Бестиарий: 8 машин, теги `precise` / `burst`

Статус: done (2026-09-03) · 8 машин ровно по таблице GDD, числа не менялись — гейты держатся с первой
итерации. `npm test` 356/356 (было 346 + 10 в `machines.test.ts`), typecheck и build чистые.
Баланс: `random medDepth=1.0 stuck=0` · `greedy medDepth=3.0 stuck=0` · `careful medDepth=3.0 stuck=0`
(careful пока равен greedy — лечения нет, отступать не за чем; это находка G10, а не регресс).
Автопилот: 0 побед / 30 смертей, гистограмма глубин `{2:14, 3:10, 4:5, 5:1}`, ни одного застревания.
· Зависит от: G2, G3 · Блокирует: G10 · Оценка: 2–3 ч · Пакет: `games/fortnight2`

## Что сделать

1. `src/content/monsters.ts`: таблица из 8 машин ровно по GDD (символ, имя, hp, кубики, speed,
   fov, поведение, полосы палуб, `salvage`, теги). Цвета — холодные, различимые попарно.
   `weight`: обычные 8–10, `H`/`A` 3–4.
2. Теги `precise`, `burst` уже читает `routeDamage` (G2) — проверить, что `source.tags`
   доходят (`spawnMonster` копирует `tags`).
3. `kindsForDepth`, `monsterBudget = 4 + floor(depth*1.5)`, палуба 1 → максимум 3.
4. Поведение `coward` уже есть; для `scout` — `stalker`. Ничего нового в `behaviors.ts`.

## Тесты (`tests/machines.test.ts`)

- На каждой палубе 1–6 доступно ≥ 2 видов; каждый вид имеет `salvage` из `MODULES`.
- Каждое `behaviour` существует в `BEHAVIOURS`.
- `x` на фикстуре бьёт самый битый модуль; `A` — по 1 всем.
- Символы уникальны; `ch` не совпадает с тайлами (`# . > + % = |`) и `@`.
- Балансовая таблица печатается (`balance.test.ts`) — записать её вывод в статус задачи.
