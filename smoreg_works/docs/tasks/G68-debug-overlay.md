# G68 — Дебаг-оверлей

Статус: done 09.09 · иначе: `` ` `` не стало членом `UiIntent` — `appReducer` в `appstate.ts` исчерпывающий и без `default`, так что новый ход туда сломал бы typecheck; вместо этого `isDebugKey` в `ui/input.ts` перехватывается в `app.ts` до `toIntent`, как уже сделано с `isViewKey`. Терминальный вид рисует блок не в холсте `rot.js` (высота там фиксирована без запаса), а отдельным `<pre>` рядом с холстом — тем же текстом, что и веб. Исключение для литералов — в `tests/purity.test.ts`, не `i18n.test.ts` (сентенс-сканер там, а не в i18n.test.ts). Зависит от: — · Блокирует: — · Оценка: 1.5 ч · Пакет: `games/salvor` · Модель: sonnet

Владелец, 8.09: «добавь в игру логов, чтобы самому разбирать и искать проблемы, тип дебаг-режим».

Чистая функция `debugLines(game): string[]` в новом `ui/debug.ts`, ничего не меняет, только читает:
уровень и счётчики тревоги (`alertState`, `systems/alert.ts:119`), для каждой машины — имя, отсек,
поведение, что помнит (`rememberRoom`), расстояние до дрона (`RoomDistance`), hp; дрон — стойка,
открытый слот (`exposureFor`, `twist/rig.ts:270`), видимость (`systems/sight.ts`), переноска
(`carriedBy`); конкурент (`systems/rivalstate.ts`), вирус (`systems/virus.ts`). Клавиша `` ` ``
переключает (`ui/input.ts`), `?debug=1` включает с загрузки (`ui/app.ts`). Рисуется отдельным
блоком под логом в обоих видах (`ui/panel.ts` / `ui/web/panel-html.ts`), моноширинно, на английском
— это не игровой текст, `t()` не нужен (исключение записать в `tests/i18n.test.ts`, если он ловит
литералы). Тест — `tests/debug.test.ts`: на фикстуре с двумя машинами оверлей называет обе, их
отсеки и расстояния; пустой при выключенном флаге. Баланса не касается.
