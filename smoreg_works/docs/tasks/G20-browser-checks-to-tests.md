# G20 — Перенести браузерные проверки во внутренние тесты

Статус: done (03.09) · Зависит от: G11 · Блокирует: G13 · Оценка: 3 ч · Пакет: `games/fortnight2`

Иначе, чем в задаче: `crashSummary` от G11 не существовало — написан здесь как
`crashSummary(seed, turn, url, error)` (URL нужен в теле карточки), `CrashReport` из `render.ts`
исчез, состояние краша — массив строк в `AppState`.

Запрос владельца (день 3): «готовые тесты из браузера перенести на внутрянку». Правило проекта:
браузер — только вью, проверка — vitest. Всё, что агенты G8/G9/G11/G15 проверяли руками в headless
Chrome, обязано стать тестами, иначе при следующей правке UI оно молча сломается.

## Что было проверено в браузере и во что это превращается

| Проверялось руками | Становится тестом |
|---|---|
| панель: `◀` едет за командой (шаг → THRUSTERS, `.` → PLATING), сгоревший слот тусклый, `ALERT` краснеет на 5 | `panelLines` систем на `newGame` + фикстуре: текст и `fg` строк после каждой команды (`tests/panel.test.ts`) |
| вспышка строки модуля в ход удара | вынести `flashSlots(prev, next): Set<number>` из `render.ts` в чистую функцию, покрыть |
| справка `?` помещается в рамку, титул целиком виден | тест на `KEY_HELP`/`TITLE_LINES`: длина ≤ ширины рамки, число строк ≤ высоты (константы из `render.ts` экспортировать) |
| титул → любая клавиша → палуба 1; `?` после смерти не стирает баннер; `shift+R` из оверлея ошибки | **вынести состояние оверлеев из `App` в чистый редьюсер** `appReducer(state, intent, game) → state` без DOM (`src/ui/appstate.ts`), `App` только вызывает его и рисует; покрыть переходы: title→playing, playing→dead, dead+help→help→dead, crash+restart→title |
| оверлей `SOMETHING BROKE`: сид, ход, URL, первая строка ошибки | `crashSummary(seed, turn, error): string[]` (есть с G11) — проверить, что покрыт; добавить кейс с не-Error |
| лог остановок автоисследования (`You see the scout.` и т. д.) | уже в `tests/auto.test.ts` — сверить список стопов с `auto.ts`, добить недостающие |
| подсказки-однократки появляются в тот ход, когда правило впервые стоило чего-то | уже в `tests/onboarding.test.ts` — добавить проверку **хода**, а не только факта |
| схема палубы: узлы на месте, `[CUR]`, рёбра | уже в `tests/deckmap.test.ts` — добавить кейс с вырезом корпуса |
| `favicon.ico` 404 | положить `games/fortnight2/public/favicon.svg` (ASCII `@` на тёмном), сослаться в `index.html`; тест — `fs.existsSync` в `tests/build.test.ts`, плюс `dist/` содержит его после `npm run build`? — нет, сборку в тестах не гонять; только наличие файла и ссылки |

## Правило на будущее

Каждая новая UI-фича сдаётся с тестом чистой функции; `render.ts` и `app.ts` — тонкие обёртки
без логики. Если логику нельзя вынести — это сигнал, что она не там лежит.

## Гейт

`npm test`, `npm run typecheck`, `npm run build`; ни одна проверка из таблицы не осталась «руками».
