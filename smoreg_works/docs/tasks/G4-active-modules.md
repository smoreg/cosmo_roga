# G4 — Активные модули: сканер, ЭМИ, сварка

Статус: done · Зависит от: G2 · Блокирует: G10 · Оценка: 2–3 ч · Пакет: `games/fortnight2`

## Что сделать

`performCommand` для `{kind:"use", slot}` — по `rig.slots[slot].kind`:

| Модуль | Эффект | Шум | Отказ (ход не тратится) |
|---|---|---|---|
| `scanner` | пометить `explored` все клетки в радиусе 14 (стены тоже), лог `Sensor pulse. The deck lights up on your map.` | `game.makeNoise(pos, 8)` | — |
| `emp` | всем живым машинам в радиусе 1 — `applyStatus(m, "stun", 3)`; заряд −1 (заряды хранить в `Slot.charges`, стартово 2); 0 зарядов → модуль остаётся, но `use` отказывает `EMP is spent.` | 6 | нет соседей: `Nothing in range.` |
| `welder` | +1 прочности **другому** целому модулю с наименьшей прочностью (не выше `integrity` из таблицы); лог `You weld the CUTTER back to 3/4.` | 5 | нечего чинить: `Nothing to repair.` |
| прочие | — | — | `That module has no active use.` |

Пустой слот → `Empty slot.` Всё через `game.rng`, ничего случайного кроме него.
`explored`-раскрытие через `game.level.explored.set` — FOV не трогать.
Клавиатура: G8 маппит `s/e/w` в `use` нужного слота (ищет слот по kind). Здесь — только sim.

## Тесты (`tests/modules.test.ts`)

- Импульс раскрывает клетки за стеной в радиусе 14 и не раскрывает на 15; шум зарегистрирован
  (`game.noise.get(pos) === 8` после хода).
- ЭМИ станит соседей и не станит на расстоянии 2; третий вызов отказывает без траты хода.
- Сварка чинит самый битый другой модуль и не чинит саму себя; не превышает максимум.
- Каждый успешный `use` подставляет свой модуль (`exposed === slot`).
