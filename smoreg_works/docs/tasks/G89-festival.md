# G89 — К сдаче на фест: дизайн 12.09, языки, обучение, звук и отдача

Статус: в работе · Зависит от: G88 · Пакет: `games/salvor` · Дедлайн джема 15.09 17:00

Владелец, 14.09: «подготовить игру для сдачи на фест · чекни переводы, чтобы ру не просачивался,
дефолтный английский · сделай обучение понятным, с подсказками, как проходить · ponytail на решения
по интерфейсу · там есть правки по дизайну · яростно правь по всем гайдам хороших интерфейсов,
звуков, отдачи · сделать конфетку».

Фиче-фриз 13.09 снят владельцем для этой волны **только в UI, звуке, текстах и обучении**. Правила
игры, числа контента, боты, пороги баланса и фикстуры не трогаются: ни одна часть не должна сдвинуть
решения бота. Если сдвинула — это ошибка части, а не повод перезаписать фикстуры.

## Источник дизайна

Проект Claude Design «Космическая игра артборды», файл `SALVOR Interface.dc.html` (живой файл сверен
с архивом байт в байт 14.09). Распакован в
`/private/tmp/claude-501/-Users-smoreg-code-my-jamrog/d856f512-2af4-409d-8f6c-460b0e1c2852/scratchpad/design/handoff/untitled/project/`,
архив — `~/Downloads/Космическая игра артборды-handoff.zip`. Читать разметку, не рендерить.
Артборды: **3a** экран с сотами целиком (строки 34–133), **3b** разобранная система и опасности
(139–288), **3c** буксир в доке (290–474), **3d** опции (476–553), **1a** титул (868–919), **1d**
подсписок двери (921–1019), **1e** док (1021–1139), **1f** карточки событий (1141–1221), **1g**
справка (1223–1276), **1h** токены и состояния слота (1278–1400); код титула с кораблями — `titleShips`
/`hullGroup`/`stars` (1694–1735). `_ds/freezino-*` — чужая дизайн-система, в игру не едет.

Комментарии владельца на артбордах: «корабли летают в космосе, а не просто узлы» (титул) · «там ещё
разные ловушки типа дыма, глянь в коде» · «удар куда и так подсвечено» (значок NEXT HIT на карте
лишний) · «окно логов больше» · «гексы на фоне корабля» (сделано) · «алерт — левый угол».

Правило G61 остаётся: **ни одного слова из артбордов в игру**. Дизайн придумывает контент («ЛЕБЁДКА»,
«ЛОВЧИЙ», верстак `a–d`, «ПРОДОЛЖИТЬ», «БЕСКОНЕЧНЫЙ РЕЙС», «ВЫХОД», LEDGER с числами, которых игра не
считает). Берётся сетка, иерархия, состояния, цвета; факты и слова — только из существующих чистых
функций и `t()`. Где дизайн назвал иначе — прав `t()`.

**Ponytail на каждое решение по интерфейсу**: самая короткая правка, честно закрывающая пункт.
Новый файл — только когда без него правка размазывается по трём чужим.

## Общие правила частей

- Свой worktree `~/code/my/jamrog-wt/g89<x>`, ветка `task/g89<x>`, `npm install` уже сделан.
- Браузер не открывать (правило владельца). Проверка — vitest на чистых функциях: строки HTML/SVG
  содержат нужные классы/атрибуты, ширины и три языка.
- `tests/purity.test.ts`: в `src/ui/web/` нет прозы — всё видимое через `t()`.
- Новые ключи i18n — во все три таблицы, **блоком сразу после якоря своей части** (ниже), чтобы
  четыре ветки не конфликтовали в одном месте.
- Коммиты мелкие и сразу, без упоминания AI, без пуша. Перед отчётом — `git merge main` в свою ветку,
  конфликты разрешить у себя, гейт на слитом: `npm test`, `npm run typecheck`, `npm run build`
  (из корня репо) — точные числа в отчёт. Балансовая строка careful из `balance.test.ts` бит-в-бит
  как на `main` (`win=0% stuck=0 sold mean=0.44 sorties med=4 turns med=197 lastHull=56%`).
- Отчёт ≤ 30 строк, нумерованный: что сделано, что сознательно не сделано и почему.
- Закончив — строка статуса своей части в этом файле.

## A — экран с сотами и панель по 3a/3b

Файлы: `src/ui/web/{styles,panel-html,screen,hex-svg,schematic-svg}.ts`, `src/ui/schematic.ts`
(типы), `src/ui/schematic-input.ts`, их тесты, `tests/fixtures/hex-svg-before-hull.svg` (перезаписать
сознательно, если рисунок меняется). В `screen.ts` — только `screenHtml`, `headHtml`, `mapHtml`
(ветка дереликта), лог; `titleCard`/`overlayHtml`/ветка буксира — часть B.
Якорь i18n: после `"alert.scuttle"`.

1. Сетка 3a: колонки `1fr 400px` (или clamp вокруг 400), строки `38px 1fr 132px`; панель на всю
   высоту справа (`grid-row: 2 / span 2`), лог только под картой, новые строки внизу; строка хоткеев
   прибита к низу панели.
2. Шапка: имя корабля · класс мелким soft · справа ход и сид (`TURN 47 · SEED …`).
3. Тревога уходит из панели в левый верхний угол карты лестницей из пяти ступеней (слово ступени,
   пройденные тусклые, текущая цветом `warn`/`bad` на 5, что делает ступень коротко) + блок
   «опасности на борту» (глиф, слово, где, чем грозит — только известные дрону). Чип кодекса `[i] N`
   живёт в том же углу. Терминал не трогать.
4. Значок NEXT HIT на карте убрать («удар куда и так подсвечено»): `exposeHtml` и его тест.
5. Строка действия в три колонки: клавиша с `▸` · метка · extra справа на той же строке; заголовки
   групп; `is-cursor`, `is-off`. Причина у выключенной строки читаема (контраст ≥ 3:1).
6. Полоса целостности в стойке по доле: полная светлая, ниже 75 % `warn`, на исходе `bad` + красная
   кромка; выжженный слот — штриховка из 1h.
7. Опасности на сотах по 3b: мороз — заливка `#101c22` + кромка по двум верхним граням + слово над
   именем; дым — заливка `#1a1a18` + кромка; мина на двери — шеврон `warn` и метка двери залита
   `warn`. Данные — поля `hazard?`/`trap?` на `SchematicRoom`/`SchematicDoor` из того же
   `hazardThings`, что уже даёт глиф. Содержимое дымного отсека не прятать.
8. Отдача и понятность карты: сота кликабельна — `cursor:pointer` и hover-обвод; `<title>` у соты с
   именем, номером и словами содержимого (слова из `things`); на кадре, где дрон получил удар
   (`flash` непуст), короткая встряска/красная кромка карты, выключаемая `prefers-reduced-motion`;
   `:focus-visible` у кнопок; `:active` у строк.

Статус: сделано 14.09 (ветка `task/g89a`). Сетка `1fr clamp(360px,29vw,440px)` × `38px 1fr 132px`,
панель на две строки, лог под картой прижат к низу, строка клавиш `sticky` снизу. Шапка: позывной ·
класс · вылазка, справа ход · сид. Тревога ушла из панели в угол карты (`cornerHtml`): строка панели
заголовком, пять ступеней (`alert.*` + новые `alert.does.*`), под ними известные опасности
(`hazardsAboard`), чип `[i] N` там же. `exposeHtml`, его CSS и ключ `panel.nextHit` удалены. Строки
действий в три колонки, выключенная — `soft` (6.3:1). Стойка: `is-worn` < 75 %, `is-low` на последнем
очке/четверти + красная кромка, `is-burned` штриховкой. Соты: `hazard`/`trap` на входе схемы из того же
`knownHazard`/`knownTrap`, что глиф; пол и кромка двух верхних граней, слово кодекса над именем, шеврон
и `warn`-метка на заминированной двери. `<title>` у соты, `cursor:pointer` и hover, `is-hit` на карте
(красная кромка + встряска, reduced-motion снимает встряску), `:focus-visible`/`:active`. Эталон
`hex-svg-before-hull.svg` перезаписан: разница — только `<title>`. Не сделано: оттенок опасностей в
виде коробок (пункт про соты), группы FIGHT/MOVE/WORK (слова артборда), вентилированные отсеки в списке.

## B — титул в космосе, док буксира, карточки конца

Файлы: новые `src/ui/web/sky.ts`, `src/ui/web/dock-html.ts`; в `src/ui/web/screen.ts` — `titleCard`,
`titleItemHtml`, `overlayHtml`, `card`, ветка `isTug` в `mapHtml` (одна строка); `src/ui/web/mount.ts`
(постоянный слой неба, чтобы анимация не перезапускалась на каждом `innerHTML`); в `styles.ts` —
секции start screen и overlays, новая секция dock в конце строки; `src/ui/render.ts` — только разбор
`runSummary` на части, если нужен для карточки. Тесты `title`, `view`, `panel-html` (карточки), новые.
Якорь i18n: после `"title.foot"`.

1. Титул 1a: звёзды и три корпуса настоящими `shipForm` (`content/hullforms.ts`) — гарпун, молот,
   кит, детерминированные сиды, медленный дрейф (CSS, выключается reduced-motion); имя крупно слева,
   строка-тэг, строки меню рамками с чипом клавиши (янтарный на курсоре), версия снизу слева, клавиши
   снизу справа. Пункты меню — существующие (`TITLE_ROWS`), придуманные дизайном не заводить.
2. Док 3c вместо `<pre class="web-board">`: те же факты, что `tugBoard`, структурой — шапка DOCK ·
   буксир · к чему пришвартован · справа строка режима; корпус на тросе (имя, тревога, системы/цена),
   вылазки и потери; стойка корпусов строками с кромкой (свой — янтарь); кредиты на руках. Слова —
   существующие ключи `board.*`/`panel.*`; новые — только подписи.
3. Карточки конца 1f: крупный заголовок, строка-причина, ряд из 3–4 чисел с подписями (кредиты, ходы,
   машины, выжжено — то, что уже считает `runSummary`), подсказка. Справка 1g: клавиши колонкой в
   акценте, если это не ломает пагинацию.

Статус: done 14.09, ветка `task/g89b` · (1) титул: новый `ui/web/sky.ts` — 210 звёзд и три корпуса
настоящим `shipForm` (гарпун 771, молот 4102, кит 20260909) с сотами корпуса бледно, дрейф вдоль киля
CSS-анимацией, `prefers-reduced-motion` её снимает; `mount.ts` держит небо отдельным слоем
`.web-sky` рядом с `.web-frame` (`display:contents`), кадр пишет только во второй — анимация не
перезапускается; видно только на `overlay: "title"`; `.web-over.is-title` без подложки, имя крупно
слева, строки меню рамками, чип клавиши янтарём на курсоре, сборка снизу слева, управление снизу
справа; заголовок `МЕНЮ` на странице не рисуется (он линует колонку терминала); (2) док: новый
`ui/web/dock-html.ts` берёт строки самого `tugBoard` группами по пустой строке — шапка (буксир +
режим справа), рамка корпуса на тросе с кромкой по тревоге от 3, стойка строками-колонками (имя ·
цена · черта, свой — янтарь), `КРЕДИТЫ` внизу (это единственный факт сверх доски, и он повторяет
строку панели — по пункту задачи); новых ключей нет; (3) карточки конца: `runSummary` разобран на
`runFigures` (терминал бит-в-бит), в вебе **пять** чисел с подписями, а не 3–4 — чтобы веб говорил
то же, что терминал (отсеки тоже); 5 ключей `end.fig.*` после `title.foot`; справка: клавиша каждой
строки таблицы в `.press` акцентом, символы и страницы те же; тест `tests/festival-web.test.ts` (14:
небо детерминировано и без слов, слой пишется один раз на фейковом DOM, док несёт каждую строку доски
на 44 буксирах × 3 языка, числа карточек равны `runFigures`, клавиши справки); лист
`title-sheet.test.ts` показывает небо.

## C — языки и обучение

Файлы: `src/i18n.ts`, `src/content/i18n/{en,es,ru}.ts`, `src/content/tutorial.ts`,
`src/systems/tutorial.ts`, `src/content/hints.ts`, `src/ui/title.ts` (только если нужно для языка),
тесты `i18n`, `tutorial`, `onboarding`, новый тест на просачивание.

1. По умолчанию английский при любом `navigator.language` и пустом хранилище (уже `DEFAULT_LANG`, —
   доказать тестом на загрузке). Проверить, что ни один путь не ставит ru сам.
2. Просачивание: прогнать боты на ≥ 50 сидах × обучение в `en` и `es`, собрать **весь видимый текст**
   (панель, список, лог, схема, титул, справка, кодекс, карточки, HTML обоих веб-видов) — ноль
   кириллицы. В `es`/`ru` — строки, совпадающие с английскими слово в слово (непереведённые), списком;
   перевести. Английский движка без ключа (`engine.*`) — тоже в список.
3. Обучение «понятно, как проходить»: цепочка подсказок ведёт за руку до выхода — на каждом шаге
   одна строка «что нажать и зачем» (клавиша/строка списка + цель), следующий шаг открывается сделанным
   действием, в конце — что делать на настоящем корабле. Замер: бот «жму то, что велит подсказка»
   проходит учебный корпус до шлюза на всех сидах обучения; до/после — сколько подсказок видит игрок и
   на каком шаге застревает. Три языка, ширины строк по тестам.
4. Обучение не шлёт карателя и не трогает баланс настоящего рейса (G88 A) — не сломать.

Якорь i18n: после ключей `hint.*`/`tutorial.*`, в конце их блока.
Статус: сделано 14.09 (ветка `task/g89c`). (1) Язык: `DEFAULT_LANG` и так `en`; тест загрузки в
`i18n.test.ts` — `navigator.language = ru-RU`, пустое хранилище → `en`, мусор в хранилище → `en`, `ru`
только после выбора игрока; скан исходников — `setLang(` и `navigator.language` нет нигде, кроме
`i18n.ts`. (2) Просачивание: новый `tests/leak.test.ts` — careful+random × 50 сидов рейса и 51 обучения
(`TUTORIAL_SEED` + 1–50), каждый 25-й ход целиком: списки и подсписки, панель, схема, лог, HTML графа,
сот и сот с `tiles`, титул во всех настройках, справка, кодекс, история, четыре концовки. `en` 1 609 521
строк (30 511 разных), `es` 1 611 126 (30 489) — кириллицы 0 до и 0 после. Непереведённое найдено и
исправлено: `JAMMED` — литерал в `systems/jam.ts` (es и ru) → ключ `panel.jammed`; четыре заголовка
кодекса штаммов (`VIRUS SPASM`/`ВИРУС SPASM` над панелью со `СУДОРОГА`) в es и ru — 8 строк. Строк
es/ru, совпавших с английским прогоном, после — 0; английских слов внутри русских строк — 0 (кроме имён,
клавиш, кодов языков, `?view=`). Строк таблиц, равных английским: es 30, ru 21 — все формат/имена или
однокоренные (`REACTOR`, `TERMINAL`, `RIVAL`, `SENSOR`, `LAB`, `EVAC`, `base`, `reac`), под allowlist в
тесте. Английского движка без ключа — 0. Попутно: русское `o разведка` на титуле → `обзор`, как на панели
и в справке. (3) Обучение: иначе, чем в задаче, — не сценарий, а та же очередь приоритетов G86, но
каждая строка называет клавишу: титул-буксир `0` вылет · `Tab` бой · строка «open» переборки, сперва
CELL, карту беречь (TERMINAL берёт SPIKE или карту, SPIKE нет, карта одна) · строка «search» тела ·
`o` обзор · строка «work» системы (только если её можно поднять отсюда) · `m` и `◆` назад к системе,
когда корабль обойдён · `<` у шлюза, только когда все три подняты · в конце «9 прыжок, 8 чартер,
0 вылет». Строка сканера убрана (ключ, который не нужен до шлюза). Последняя строка вернулась на
продажу корпуса: на любом возвращении она говорила «урок окончен, прыгай» дрону с двумя тёмными
системами. Замер — бот-«читатель» в `onboarding.test.ts`: знает три фразы (`press K`, `press its W
line`, `press K, then a line marked M`), жмёт через настоящие `toIntent`/`appReducer`, сначала дело
в отсеке, потом ходьба, дважды ждёт ход новой строки. До: 0 из 25 (`TUTORIAL_SEED` + 1–24), застрял на
буксире на ходу 0 после 2 подсказок; с подаренным вылетом — 0 из 25, у скаута в доке после 5. После:
25 из 25 продают корпус и дома, 13.1 подсказки за забег, застреваний 0. Три языка: тест, что es/ru
строки называют те же клавиши и слова своих строк списка (`activar`, `подъём`…); ширины 93 держатся.
Метрики G86 после: урок про машину у первого удара 24/24 у careful/greedy/random, про переборку
24/24 × 3, двух подсказок на ход 65/304, 46/248, 51/171; финальная строка — всем продавшим корпус
(23/23, 1/1, 0/0; боты подсказок не читают). (4) Каратель и баланс: `alert.ts` не тронут, система
обучения инертна вне обучения; careful бит-в-бит `win=0% stuck=0 sold mean=0.44 sorties med=4 turns
med=197 lastHull=56%`, фикстуры `voyage-*.json` не менялись. Сознательно не сделано: кириллица в
CSS-комментариях `ui/web/styles.ts` (браузер их не показывает, файл части A); обучение не учит
чартеру делом — только называет `8` в последней строке. Гейт на слитом: `npm test` 2253/2253,
`typecheck` и `build` зелёные.

## D — звук и отдача

Файлы: `src/ui/sfx.ts`, новый `src/ui/uisound.ts`, `src/ui/app.ts`, тесты `sfx`, новый `uisound`.
Не трогать `ui/web/*` (это A и B), `packages/audio` только чтение.

1. Аудит: какие ключи лога на 200 сидах ботов важны и молчат (подъём тревоги `log.alert.up`, знак
   опасности `log.hazard.tell.*`, отсчёт подрыва, вентиляция, мина, закрытая кораблём дверь, первая
   поднятая система, прибыль у шлюза, гибель ядра) — таблица до/после, частота на ход. Карта ключ →
   существующий звук, без новых файлов, `MAX_PER_TURN` оставить.
2. Интерфейсные звуки без файлов — WebAudio-синтез коротких блипов: ход курсора, принять строку,
   отказ (`effect.kind === "log"` от нажатия выключенной строки), открыть/закрыть карточку, смена
   языка/вида. Чистая таблица «интент → блип (частота, длительность, огибающая)» под тестом, тонкий
   плеер с тем же выключателем `S`/`?sound=off`, громкость ниже эффектов, ничего не звучит до первого
   жеста. Не звенеть на каждом шаге авто-исследования.
3. Музыка: не трогать логику, проверить, что `S` глушит и блипы.

Статус: сделано 14.09 (ветка `task/g89d`). Аудит 200 сидов × 3 бота (136 926 ходов бота): ходов со
звуком 47 487 → 62 942; 49 из 88 молчавших ключей разложены на существующие 13 эффектов, 39
оставлены молчать сознательно (подсказки, отказы движка, укрытие, контакты, текст чартеров) (`log.alert.up`
49/1000 ходов → alert, `log.hazard.tell.*` → pulse, вентиляция и мина → emp, дверь кораблём → door,
`log.credit`/`log.carry.home` → sold, `log.system.all` → system, подборы → salvage). Найдено и
починено: звук слушал лог по счётчику, а `MessageLog` держит 200 строк — после 200-й строки игра
молчала до конца рейса (32 430 из 62 942 озвученных ходов терялись), и после `shift+R` тоже.
`MAX_PER_TURN` = 3 оставлен, но срезается по важности (`SFX_RANK`): стингер гибели дрона выпадал 56
раз из 1 580, теперь 0. Блипы — `ui/uisound.ts`: таблица из 8 (tick, step, accept, back, refuse,
open, close, toggle), правило `blipFor` и тонкий WebAudio-плеер, всё под `tests/uisound.test.ts`;
тот же выключатель, что у музыки и эффектов; ни одного блипа из таймера авто-исследования и поверх
игрового звука.
