# G65 — Меню буксира: семь правок из аудита

Статус: done 10.09 · Зависит от: G53, аудит `docs/tug-menu-audit.md` · Блокирует: G67 · Оценка: 3 ч · Пакет: `games/salvor` · Модель: fable

Сделано П1–П4, П7, чек-лист вылета и переименование `action.pick.fit`; П5 и П6 не трогал (чужие
файлы). Иначе, чем в задаче, вышло два места: **(1)** параметр `floor` у `tugPicks` не понадобился
— порог входа в подсписок живёт не там, а в `tugRow` (`offers.length === 1`), и его снимает П2,
так что `tugPicks` просто требует непустой список; **(2)** чек-лист вылета не влезает в
`ACTION_WIDTH = 25` в том виде, что в задаче (`вылет на ARGENT · 2 модуля битых · чартер не
подписан` — 50 знаков, а самый длинный позывной 13), поэтому позывной уступает строку списку: пока
что-то не сделано — `вылет 2 битых, без работы` (25 знаков в русском, 22 в английском), когда
сделано всё — прежнее `вылет на ARGENT`; позывной при этом остаётся на баннере и на панели.
**П7 сделан и оставлен:** баланс не шелохнулся, до и после совпали знак в знак —
`careful runs=32 win=3% stuck=0 sold mean=0.38 sorties med=5 lost med=3 turns med=227 banked med=51
online1 mean=2.16 charters mean=2.41 lastHull=50%`, фикстуры не перезаписывались, потому что боты
идут по `STATION_ORDER` в `systems/voyage.ts`, а не по `TUG_ROWS`.

Владелец, 9.09: «продумай как опытный геймдиз меню на корабле, пока оно очень неудобно: тип нельзя
взять все контракты, после 2 из 3 выкинет на основное меню и т. д.». Разбор сделан агентом
9.09 без правки кода — **`docs/tug-menu-audit.md`**: восемь дефектов с воспроизведением, семь
предложений П1–П7 с ценой, и список «что сделал бы геймдизайнер за полдня». Задача — исполнить
его. Читать аудит целиком до кода.

## Корень жалобы — одна строка

`ui/actions.ts:435`, `tugPicks`: `if (offers.length < 2) return undefined` — тот же порог служит и
**входом** в подсписок (`tugRow`, 403), и его **жизнью** (`tugStands`, 444 → `levelStands` в
`ui/appstate.ts:729`). Подписал два чартера из трёх — целей осталась одна — уровень «не стоит» —
выброс на главный список, курсор на `вылет` (дефект 2 аудита: пятый Enter отправляет в рейс).
То же в ремонте, продаже, трюме.

## Что сделать — в порядке аудита

| # | Правка | Где | Ломает |
|---|---|---|---|
| П1 | уровень стоит, пока есть хоть одна цель: `tugPicks(game, verb, floor = 2)`, `tugStands` зовёт с `floor = 1` | `actions.ts:435–447` | ничего |
| П2 | строка с `nest: true` всегда открывается списком — убрать `offers.length === 1` из схлопывания | `actions.ts:414` | одно нажатие там, где цель одна; `sell` перестаёт продавать последний модуль в одно нажатие |
| П3 | подсписок собирает те же цели, что строка (`verb` + `also`) — вынести сбор в одну функцию, звать из `tugActions` и `tugPicks` | `actions.ts:362–367, 438` | исключение для `order` в `tug.test.ts:296` снять |
| П4 | `why` серой строки — от своего глагола, иначе `t(row.empty)`; строке трюм+полка свой ключ `why.hold.shelf` | `actions.ts:430`, i18n ×3 | ничего |
| П7 | порядок групп по порядку визита: ДРОН → ЧИНИТЬ → СНАРЯЖЕНИЕ (снять / поставить-купить / продать) → КОНТРАКТЫ → СМЕНА ТОЧКИ → ВЫЛЕТ последним; заголовок над единственной строкой не рисовать | `TUG_ROWS`, `actions.ts:342` | `tug.test.ts:211` (список заголовков) переписать; **баланс — см. ниже** |
| 5 | строка вылета — чек-лист: `вылет на ARGENT · 2 модуля битых · чартер не подписан` | `stationTargets`, ветка `undock`, `systems/voyage.ts:~1800`, i18n | ничего |
| — | переименовать `action.pick.fit` («поставить из трюма ▸» → «трюм и полка ▸») на трёх языках | i18n | фикстуры, если бот читает метку — нет, он жмёт по `enabled` |

**Отложено до слияния волны** (двухстрочные, но в чужих файлах): П5 — мышь шлёт позицию, а не
цифру (`ui/web/panel-html.ts:251`, файл у G63); П6 — курсор после схлопывания уровня
возвращается на строку группы (`ui/appstate.ts:729–733`, файл у G64). Сделать их одним коммитом
после мержа G63/G64, это 15 минут.

## Файлы во владении

`ui/actions.ts` — **только половина буксира** (стр. 330–470: `TUG_ROWS`, `tugActions`, `tugRow`,
`tugPicks`, `tugStands`); G64 добавляет в `roomActions` (230) одну строку — не конфликт ·
`systems/voyage.ts` — только ветка `undock` в `stationTargets` (полку и `buyModule` не трогать,
они доделаны 9.09) · `content/i18n/{en,ru,es}.ts` (свои ключи) · `tests/tug.test.ts`,
`tests/actions.test.ts`, `tests/onboarding.test.ts`.

**Не трогать:** `ui/appstate.ts`, `ui/input.ts` (G64), `ui/web/*` (G63), `twist/rig.ts` (G66).

## Тесты (`tests/tug.test.ts`, образцы — `picks(game, "sell")` там же)

1. Три чартера на доске: подписать все три, не выходя из группы; после третьего уровень падает
   на строку группы (это уже П6, поэтому — «падает», без проверки курсора).
2. Три битых модуля: починить все, не выходя из группы; кредиты списаны трижды.
3. Одна цель в группе с `nest` — всё равно список из строки + «назад» (П2).
4. Трюм+полка: при целом дроне и пустом трюме подсписок даёт три серые строки полки с их
   собственными `why`, строка группы серая с `why.hold.shelf` (П3+П4).
5. Заголовки в новом порядке, вылет последним; `onboarding.test.ts:161` ищет вылет по глаголу —
   проходит без правки.
6. Строка вылета перечисляет ровно то, что не сделано, и пуста, когда сделано всё.
7. «Ни одна строка не потерялась при переезде» (тест уже есть) — зелёный.

## Баланс

П1–П4 и чек-лист вылета баланс не трогают: боты жмут `enabled`-оферты, не читают меток и не
живут внутри уровней (`roomPlay` зовёт `stationTargets`, а не `roomActions`).

**П7 — трогает.** Три generic-правила `roombots.ts` ведут бота по буксиру до `undock` в порядке
списка оферт; перестановка групп меняет, что бот делает первым. Делать П7 **отдельным коммитом**
после П1–П4: снять базу (`balance.test.ts` + `winnable.test.ts`, четыре числа: уведено из 32 /
медиана ходов / дошли до последнего % / побед на 200; ориентир 10 / 222 / 53 % / ≥ 3), применить,
перезаписать фикстуры `RECORD=1`, сравнить. Порог тот же — все `expect` обоих файлов зелёные, побед
≥ 3 на 200. Если П7 роняет планку — оставить старый порядок, записать таблицу в `Статус:` и
сказать об этом; порядок групп не стоит трёх побед из двухсот.
