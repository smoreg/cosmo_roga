# G88 — Полировка по карте проблем 11.09

Статус: в работе · Источник: `docs/problem-map-2026-09-11.md` (полная версия — `~/reports/salvor-problems.html`)
· Владелец: «полируй по отчёту, делай» · Фиче-фриз 13.09

Шесть пунктов плана, не зависящих от решений владельца. **Не делать** (ждут владельца): планку
проходимости (период тревоги), гараж G67, «уход с корабля −3 ступени».

Базa на `main` @ 52df018: careful/32 — win 0 %, sold mean 0.44, sorties med 4.5, turns med 197,
lastHull 56 %; побед careful на 200 сидах — 8; уведено 14/32. Планка: побед ≥ 3/200, уведено ≥ 10/32,
буксир отца 10–80 %, все `expect` в `balance`/`winnable`/`smoke`, `stuck === 0` — **не ослаблять**.

## A. Бот, обучение, трюм (fable, `task/g88a`)

Статус: done 11.09 · четыре коммита по одному на пункт: 255e049 защёлка · 48dfb8e обучение ·
f0be07a трюм · 6c5396c порядок строк. Иначе, чем в задаче: (1) защёлка — одно поле `spent` в
`makeCarefulBot`, сброс при смене корабля и при `replaced` (новый корпус), `spent ||= failing(…)`;
(2) `isFirstShip` считает первым **и** учебный, **и** корпус сразу после него — учебный сохраняет
темп 80 и пустые ступени 2–3 (там лестница читается впервые), только каратель на него не приходит
(оба вызова `sendEnforcer`); функция экспортирована, `hazards.ts` читает её вместо своей копии;
(3) `bonus` несётся через `Carried`, трюм, руки и пол (`swapIn`, `unload`, `stow`, `fit`,
`autoFit`), `makeSlot` клипует его до `MAX_GRAFT`, строка `log.hold.fit` печатает прочность со
стойки, а не из трюма; (4) строка «В СЕТИ» уходит в лог до `standDown` и до шага тревоги, порядок
под тестом в `ship.test.ts`. Замеры (careful, `measure.spec`, 1500 шагов): ч. 1 — побед на 200
8 → 12, у лимита 1 → 0, 32 сида те же; ч. 2 (`TRAINING=1`) — побед 6 → 15, первый настоящий
корпус продан в 19 → 64 рейсах, учебный в 142 → 198, обычные рейсы бит-в-бит; ч. 3 — careful/32:
sold mean 0.44, turns med 197, lastHull 56 % те же, CR med 16.5 → 15, sorties med 4.5 → 4,
banked med 55 → 52, stuck 0; побед на сидах 1–200: 12 → 7, на 201–600 (A/B дерево ч. 2 против
ч. 3): 13 → 12, у лимита 2 → 2 — просадка первого окна это шум по сидам, не регресс; ч. 4 баланс
не трогает. Фикстуры `voyage-*.json` перезаписаны на слитом дереве дважды (после ч. 3 и ч. 4).
Гейт на каждой части: `npm test` зелёный (2410 → 2414 → 2416 → 2416), `typecheck`, `build`.

1. **Защёлка бота** (`packages/engine/src/testing/roombots.ts`, `failing()` ~378, вызов ~650): мороз
   снимает 20 скорости, бот видит «слабею», уходит; за дверью скорость вернулась — возвращается;
   цикл до лимита. Решил уходить — уходит до смены корабля или стойки. Замер роя: сиды 1–32 бит-в-бит
   те же, побед 8 → 12, застреваний 0 на 200.
2. **Обучение не портит рейс:** `isFirstShip` (`systems/alert.ts:1012`) и `isFirstDerelict`
   (`systems/hazards.ts:144`) не должны считать учебный корпус первым — первый **настоящий**
   корпус получает темп 80 и «без опасностей»; учебный корабль не присылает карателя. Замер роя:
   7 → 15 побед в рейсах с обучением, первый корпус продан в 64 рейсах против 18.
3. **Наращивание в трюме:** трюм не хранит `bonus` (`systems/voyage.ts:346`), `stow` зовёт
   `carriedFrom` без него (`:1076`, `twist/rig.ts:779`), `fit` печатает старую прочность
   (`:1137/1143`). Нести `bonus` так же, как уже несётся `base`. Воспроизведение: `newGame(1)`,
   999 CR, `graft` слот 0 → `stow` → `fit` → 11/11 вместо 12/12, в логе «(12)».
4. **Порядок строк:** «Корабль обезврежен» печатается раньше «ПРИВОД В СЕТИ» (`alert.ts:759`,
   `ship.ts:172`) — причина первой.

## B. Интерфейс и понятность (opus, `task/g88b`)

Статус: done 11.09 · 40d8b6b клик и титул · b72b9f4 тест обучения (строка A, по слову ведущего) ·
bf3337e «нечем», строка систем, подвал буксира · bdda05c вылет на буксире, метка `◆`. Иначе, чем в
задаче: (1) у соседа за известной опасностью ходьбой со стопом стала и строка двери в списке отсека,
не только клик и `m`: клик нажимает эту строку, так что правила не расходятся; (2) с титула `.`
отчаливает вместе с пробелом (одно намерение), `title.start` поправлен на месте, а не новым ключом
(его читает `i18n.test`); (3) «нечем» — строка на каждую застрявшую систему
`ТЕРМИНАЛ: ОТМЫЧКА/ключ-карта`, строка `< на выход` в этом состоянии снята: её ряд отдан строке
систем, `<` и так в подвале; (4) пустые ряды между блоками панели уступают раньше блока цели и
контактов — иначе строка систем в два ряда прятала адреса на 44.8 % экранов; (5) на буксире список
получает все свои ряды и ряд счёта при одиннадцатой строке, после воздуха уступают строки
подписанных чартеров; (6) метка цели в `m` — `◆`, `+` остался схеме и стойке. Замеры (careful,
150 сидов, ru; клики — 60 сидов, en), было → стало: молчащих кликов по нарисованному соседу 1707 из
1707 → 0; вход кликом в известную опасность без стопа 96 из 262 → 0 из 284; «нечем» называет каждую
систему с инструментом 1.2 → 96.3 %; открытые системы без строки систем 25.0 → 7.0 %; система как
`?` 61.0 → 0 %, короткие формы 24.5 → 0 %; двойной `0` на буксире 65.8 → 0 %; вылет спрятан 34.2 →
0 %; список обрезан счётом на борту 14.6 → 9.6 %. Баланс не двигается: careful/32 win 0 %, sold mean
0.44, turns med 197, lastHull 56 %, stuck 0; фикстуры не перезаписаны. Гейт на слитом дереве на
каждой части: `npm test` 2427 → 2428 → 2429 / 119 файлов, `typecheck`, `build` (543.25 kB).

1. **Клик по неразведанному соседу** ничего не делает (`ui/actions.ts:594`, `ui/appstate.ts:750`):
   соседний отсек через дверь — шаг, как строка списка; через известную опасность — стоп и
   подтверждение (правило G83). И то же правило **на любом расстоянии**: сейчас сосед с опасностью
   открывается сразу без стопа, а через две двери — со стопом (`actions.ts:631`).
2. **Справка с титула через историю:** `PageUp` → `Esc` пускает в рейс в обход `started()`, флаг
   `titleHelp` остаётся взведён (`appstate.ts:798–913`, `historyTurned`, `closedHistory`).
3. **Буксир:** `Tab`/`o` при открытой справке закрывают карточку, а не пишут за ней
   (`appstate.ts:767`); «любая клавиша» на титуле — только цифры, `Enter`, пробел (`:597`).
4. **«НЕЧЕМ ПОДНЯТЬ»** называет систему и инструмент (`НЕЧЕМ: ТЕРМИНАЛ ждёт ОТМЫЧКУ или
   ключ-карту`, в две строки, если не влезает) и не прячет строку систем (`ui/panel.ts:410`).
5. **Строка систем** полными словами (в две строки при нужде) вместо `?прив ·реак r16`; `?` →
   «не найден» (`systemsLine`).
6. **Двойной `0` на буксире:** на верхнем уровне подвал без `0 назад` (`ui/panel.ts:1012`).
7. **Терминал прячет вылет и прыжок** под «… ещё 2» на 35 % экранов буксира: блок чартеров на
   панели уступает место первым (`panelBlocks`).
8. Метка цели `+` в меню хода совпадает с `+` наращивания в стойке (`actions.ts:681`) — сменить
   один из знаков.

## C. Тексты, кодекс, чартеры (opus, `task/g88c`)

Статус: done 11.09 · 37fb6c0 тексты, кодекс, чартеры · 3dd58da фикстуры. Иначе, чем в задаче:
(1) `ПОДРЫВ через N` в 28 колонок не влезает (29), а веб-панель узнаёт строку тревоги по слову
`ТРЕВОГА` в начале — стало `ТРЕВОГА ▮▮▮▮▮ ДО ПОДРЫВА 15`; цель буксира отца — ключ `panel.goal.bare`
(цена 0), полная фраза 45 колонок, стало `ЦЕЛЬ  3 СИСТЕМЫ, УЙТИ ЖИВЫМ`; (3) «откупиться» не
тронут — подлежащее в нём есть, переписаны отход (`уйти: чужой платит 100 CR`) и дележ
(`отдать чужому полпродажи`); (4) строка «не выполнен» у шлюза — для всех трёх малых чартеров
(УТИЛЬ с `есть/нужно`, ДОСТАВКА, ПЕРЕДАЧА), на каждом возвращении, ПОДЪЁМ молчит — его говорит
цель; строка прыжка при поднятых системах уступает имя корпуса, как строка вылета
(`бросить 2/3, продажа 200` — в ru без CR, иначе 27), при дележе — половина цены; лог прыжка
называет все неоплаченные чартеры, ПОДЪЁМ тоже, если корпус не продан; вылет говорит
`— доска закроется` только когда доска — единственный долг, с поломками остаётся `без работы`
(ru уже 25 из 25). По слову владельца поправлены литералы в чужих тестах, только ожидаемая
строка: `panel.test` 681 и 1049, `actions.test` 897, `onboarding.test` 186 и 194. Скриптов
текстового агента роя по указанному пути нет — работал по таблице `~/reports/salvor-problems.html`.
Гейт на слитом дереве (A влит): 2423 теста / 119 файлов, `typecheck`, `build`; careful/32 — win 0 %,
sold mean 0.44, sorties med 4, turns med 197, lastHull 56 %, stuck 0 (тексты баланс не двигают).

1. Мелочи G87, часть 4 которой не вернулась: `panel.keys` ru `КАРТЫ` → `КЛЮЧЕЙ`;
   `panel.contact.hit` ×3 `удар:` → `выжжет:` / `burns` / `quema`; `action.fit` ×3 со знаменателем
   (`СВАРКА 1/5`, вызов `voyage.ts:1981` передаёт `max`); `action.purge` ru со счётчиком ходов;
   `panel.alertScuttle` ru `ПОДРЫВ через {n}`; `action.charter` es в скобках; `why.charter.late`
   ru/en «доска чартеров закрыта — борт уже открыт»; строка цели на буксире отца «подними три
   системы и уйди живым»; заголовок корабля режется по слову (`ui/schematic-input.ts:544` `clipTo`).
2. Карточки кодекса: каратель, охранник, мусорщик (`content/codex.ts`, три языка).
3. Сделки с конкурентом с подлежащим (`ru.ts:380–382` и en/es): «чужой платит 100 — уйти с
   корабля», «отдать чужому половину продажи».
4. Чартеры: у шлюза «ДОСТАВКА не выполнена: ящик остался на борту» (`voyage.ts` `payCharters`
   ~686); строка прыжка «бросить 2/3, продажа 200 CR» и «бросаешь: ДОСТАВКА» на прыжке
   (`voyage.ts` ~1358 и ветка `jump` в `stationTargets`); вылет без чартера «доска закроется»
   (`castOffLeft` ~1788).

## D. Код до фриза (после слияния A–C, отдельным агентом)
Пункты 1–9 ponytail-отчёта: `games/fortnight2/**` в `exclude` vitest; мёртвые экспорты; ключ
`HUNTER_LEVEL` в `LADDER`; `stationGuide`, `droneLost`/`shipSold`, `sizecheck.test.ts`;
`creditsOf` и `machinesIn` дубли; врущие комментарии. Поведение не меняется.

Статус: done 11.09 (`task/g88d` @ 0f7bb64) · девять коммитов, один на пункт: 4dfa992 fortnight2
из exclude · 1faab8f шесть мёртвых экспортов (`alertOff`, `unloadCarried`, `STOCK_SIZE`,
`MIN_MACHINE_FOV` салвора, `CORE_HP` салвора, `hexCorners`/`inside` из `hullforms.ts` — у
второго `hexCorners` есть тёзка в `ui/web/hex-svg.ts`, использующийся, не трогали) · 5c170f3
`creditsOf` в `rival.ts` заменён на импорт из `purse.ts` (лист, цикла нет) · bbc0b76 `LADDER`
берёт ступень охотника из `HUNTER_LEVEL` (`N >= HUNTER_LEVEL` вместо литералов) · 27dc8e3
`stationGuide`/`StationRow` убраны вместе с утверждением в `tug.test.ts` и своим упоминанием в
комментарии `panel.ts` · 5e6b574 `droneLost`/`shipSold` убраны вместе с их единственным
изолированным тестом (остальные тесты того же describe уже проверяют настоящий путь через
`raisedBy`/`syncStatus`) · ff25d8d `sizecheck.test.ts` удалён · c97032e `machinesIn` в
`ui/actions.ts` заменён на `hostilesIn` из `twist/rig.ts` (тот же код); одноимённый в
`ui/schematic-input.ts` не трогали — другое поведение (не фильтрует фракцию), и коллизия имён
пропала сама, раз в `actions.ts` своего `machinesIn` больше нет · 0f7bb64 враньё в комментариях:
`.claude/CLAUDE.md` 198 → 2182 теста, `README.md` ~1600 → 2182 (+ пометка про fortnight2 отдельно);
две строки счёта строк в `docs/adr/0003-decoupling.md`; `cards.ts`/`doors.ts` — «until G18»/«until
G26» переписаны на факт (обе задачи закрыты недели назад); `tIn` в `i18n.ts` и комментарии
`ui/web/screen.ts` — называли `helpBody`/`TITLE_LINES`, которых там больше нет.

Время `npm test`: 119 файлов/2429 тестов/48.38 с → после п.1 98/2184/23.77 с → после п.2–9 (мёртвый
код и один тест меньше) 97/2182/19–21 с. Гейт: `npm test` 97/2182 зелёные, `typecheck` чисто,
`build` чисто (543.17 kB). careful/32: `win=0% stuck=0 sold mean=0.44 sorties med=4 turns med=197
lastHull=56%` — совпадает с планкой день-в-день. Фикстуры `voyage-*.json` не трогали.

## Владение и пересечения
- A: `packages/engine/src/testing/roombots.ts`, `systems/alert.ts`, `systems/hazards.ts`,
  `systems/ship.ts` (порядок строки), `content/tutorial.ts`/`systems/tutorial.ts` (каратель),
  `systems/voyage.ts` **только трюм** (`hold`, `stow`, `fit`, `autoFit`), `twist/rig.ts`
  (`Carried`, `carriedFrom`, `makeSlot`), тесты `rig`, `tug`, `alert`, `hazards`, `onboarding`,
  `tutorial`, `balance`, `winnable`, `smoke`, фикстуры `RECORD=1`.
- B: `ui/actions.ts`, `ui/appstate.ts`, `ui/panel.ts`, `ui/input.ts`, `ui/title.ts`, тесты
  `actions`, `appstate`, `panel`, `panel-html`, `title`, `input`; i18n только новые ключи `panel.*`/
  `why.*` для своих строк.
- C: `content/i18n/{en,ru,es}.ts`, `content/codex.ts`, `ui/schematic-input.ts` (только `clipTo`),
  `systems/voyage.ts` **только строки** (`payCharters`, `jump`, `castOffLeft`, вызов `action.fit`),
  `systems/rival.ts` (только ключи сделок), тесты `i18n`, `codex`, `tug`, `voyage`, `rival-bargain`.
- `systems/voyage.ts` делят A и C по разным участкам; `i18n` делят B и C по разным ключам —
  конфликты при мерже тривиальные, разрешать у себя после `git merge main`.
