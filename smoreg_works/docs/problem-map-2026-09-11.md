# Карта проблем SALVOR — 11.09.2026, main @ 1fcbe9f

Рой из пяти агентов: крэши и тупики, понятность, баланс, тексты, код (ponytail). Код не менялся,
всё мерено ботами и чистыми функциями на 150–400 сидах. Полная версия с таблицами —
`~/reports/salvor-problems.html`.

## Итог
- **Блокеров в игре нет.** 0 исключений на 400 прогонах / 87 тыс. ходов, тупиков 0, сейв бит-в-бит,
  потолок `CROWD = 3` держится.
- **Понятность:** игрок без букв доходит до системы в 82/150 (было 0). Осталось «где и чем».
- **Баланс:** careful 8/200 побед, 92/200 рейсов без продажи.
- **Гараж G67** откачен (`1fcbe9f`): ослаблял `stuck === 0`. Путь к слиянию замерен, см. ниже.

## План до фриза 13.09 (≈ 8–9 ч, пункты 1–6 независимы)
1. **Защёлка в боте** (`packages/engine/src/testing/roombots.ts:650`): решил уходить — уходит до
   смены корабля. Мороз снимает 20 скорости, бот видит «слабею», уходит, возвращается — цикл. Сиды
   1–32 бит-в-бит те же, побед 8 → 12, застреваний 0. Это же единственная причина застреваний гаража.
2. **Обучение не портит рейс:** учебный корабль берёт себе «первый корабль рейса»
   (`systems/alert.ts:1012 isFirstShip`, `systems/hazards.ts:144 isFirstDerelict`), и настоящий
   первый корпус теряет темп 80 и защиту от опасностей; плюс учебный шлёт карателя. Обе правки:
   7 → 15 побед в рейсах с обучением.
3. **Честность:** наращивание теряется через трюм (трюм не хранит `bonus`, лог врёт —
   `voyage.ts:346/1076/1137`); клик по неразведанному соседнему отсеку ничего не делает
   (`actions.ts:594`, `appstate.ts:750`); справка с титула через `PageUp` → `Esc` пускает в рейс в
   обход старта (`appstate.ts:798–913`).
4. **Понятность за час:** «НЕЧЕМ ПОДНЯТЬ» называет систему и инструмент и не прячет строку систем;
   на буксире убрать `0 назад` рядом с `0 вылет`, прыжок пишет «бросить 2/3»; строка систем полными
   словами вместо `?прив ·реак r16`.
5. **Тексты и кодекс:** мелочи G87 (часть 4 не вернулась): `КАРТЫ` → `КЛЮЧЕЙ`, `удар:` → `выжжет:`
   ×3, заголовок режется посреди слова, `СВАРКА 1` → `1/5`, `прожиг` без счётчика ходов в ru,
   `ПОДРЫВ через N`; карточки кодекса карателю, охраннику, мусорщику; сделки с конкурентом с
   подлежащим; провал чартера у шлюза.
6. **Код до фриза (~110 строк мёртвого, ≈ 1 ч):** исключить `games/fortnight2/**` из прогона
   (треть времени тестов); мёртвые экспорты `alertOff`, `unloadCarried`, `STOCK_SIZE`,
   `MIN_MACHINE_FOV`, `CORE_HP`, `hexCorners`, `inside`; `HUNTER_LEVEL` не читается игрой, но тесты
   гоняют тревогу через него; заглушки `stationGuide`, `droneLost`/`shipSold`, `sizecheck.test.ts`;
   врущие комментарии.

## Решения владельца
1. **Планка:** А — период 40 + защёлка (12/200 побед); Б — период 80 (23/200); В — период 100 +
   кулдаун шума 15 (66/200, но до буксира отца 82 % на 200 сидах — выше потолка 80).
2. **Гараж:** защёлка + `GARAGE_LIMIT` 2 + вернуть пять ослабленных порогов → всё зелёное, кроме
   медианы прогресса в `smoke` (13.5 при пороге 16). Снизить порог до 13 или отложить гараж.
3. **Уход с корабля −3 ступени вместо −2** (возвращение на ступень 3 убивает в 87 %): замерить
   или оставить.

## Закрыть без правок
- Пункт 6 G85 (лестница тревоги): по вылазкам (одна расстыковка) все пять ступеней ≥ 5 %
  (2 / 7 / 11 / 23 / 21 / 36 %). Мерили не той единицей.
- Опасности: гибель в морозе 61 %, в дыму 58 %, без опасностей 59 % — правило G71
  «ожидаемая цена ноль» выполняется.
