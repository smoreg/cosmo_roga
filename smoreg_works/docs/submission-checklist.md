# Чеклист сдачи (день 13–15)

Про `games/salvor` — то, что сдаётся. Правило одно: **строка либо доказана названным тестом,
либо помечена «владелец»**. «Зелёный `npm test`» без имени файла ничего не доказывает: через
неделю никто не вспомнит, какой именно тест это ловил.

`[x]` — доказано на текущем `main`. `[ ]` — не сделано либо сделать может только владелец.
Числовых порогов баланса здесь нет намеренно: они живут в `games/salvor/tests/balance.test.ts`
и меняются, а чеклист, который приходится править вслед за числом, перестают читать.

## Технически

- [x] `npm test` — зелено (гейт перед словом «готово» в каждой задаче)
- [x] `npm run typecheck` — зелено
- [x] `npm run build` — собирается
- [x] **Fuzz без единого исключения** — `tests/fuzz.test.ts`: 200 сидов × 500 случайных команд
      боевой сборкой плюс 100 × 500 со счётом-полом (иначе бюджет фаза уходит на банкротство в
      первые семьдесят команд). Крашей ноль в обоих прогонах
- [x] **Боты доводят забег до конца, не роняя игру** — `tests/smoke.test.ts` (три бота × 32 сида
      на первом грузовике), `tests/winnable.test.ts` («ends every voyage the two ways the
      document allows»)
- [x] **Забег кончается по правилам, и экран конца это показывает** — `tests/appstate.test.ts`,
      блок «the endings»: баннер поднимается в тот же ход, победный отличается от смертного,
      после конца ходы не тратятся
- [x] **Перезапуск с экрана конца** — `Shift+R`, `tests/appstate.test.ts` («starts a new run»);
      новый сид уходит в адресную строку (`src/ui/app.ts`, `newRun`)
- [x] **Размер бандла** — `dist` 1.5 МБ, `salvor-web.zip` ≈ 1.0 МБ при пороге 2 МБ (сборка
      `wave/g90`, 15.09). Кода — 644 КБ (196 КБ gzip), остальное — 17 файлов `.ogg`: трек
      (598 КБ) и 16 эффектов. Трек в zip не жмётся (`deflated 0%`)
- [x] **Перехват исключения в UI** — `src/ui/app.ts`: каждое нажатие и каждая перерисовка идут
      через `guard`, плюс слушатели `error` и `unhandledrejection`. На экран выводится карточка
      `crashSummary` (`src/ui/appstate.ts`): сид, вылазка и корпус, номер хода, **полный URL с
      `?seed=`** и строка «copy this URL and report it». Проверено `tests/appstate.test.ts`,
      блок «the error screen»
- [x] **Реплей бит-в-бит и защищённый разбор сейва** — `tests/replay.test.ts`: записанные забеги
      воспроизводятся по отпечатку, испорченная запись отвергается с причиной, и игра после
      этого живёт
- [x] **Запись рейса переживает JSON** — `tests/persistence.test.ts` («the run's record is data
      a save could carry») и `tests/fuzz.test.ts`: ни одного поля-функции, ничего не теряется на
      круге через `JSON.parse(JSON.stringify(…))`
- [ ] Десять полных забегов руками без единой ошибки в консоли — **владелец**

## Игра

- [x] **Управление объясняется на первом экране** — титул несёт строку клавиш (`titleLines()`,
      `src/ui/input.ts`), и она проверяется на ширину и содержание в `tests/input.test.ts`
      («keeps the title card on the screen», «says what the game is on the title»)
- [x] **`?` открывает справку, `Esc` закрывает** — `tests/appstate.test.ts` («the help card»);
      каждая строка справки влезает в кадр во всех трёх языках — `tests/input.test.ts`,
      `tests/i18n.test.ts`
- [x] **Первые два нажатия работают на каждом сиде** — `tests/onboarding.test.ts`: вылет одним
      нажатием списка на каждом сиде («casts off in one press of the list»), буксир — девять строк
      в одном порядке, выбор первого корпуса и вылет среди них, у каждой серой строки есть причина
      («puts every verb of the tug on the first screen», «opens on ten rows…» — в имени старое
      число, проверяет девять)
- [x] **Первый корпус учит, а не убивает** — `tests/onboarding.test.ts`, «the first freighter, on
      200 seeds»: каждый стартовый корпус первой точки ведёт с контрактом УТИЛЬ, рейс подписан на
      него, 25 CR в кассе; дрон приземляется на обломки, с одной запертой дверью и ключом перед ней,
      первая машина показывается в первые десять ходов
- [x] **Твист объясняется в тот ход, когда он впервые случается** — пять однократных подсказок,
      `tests/onboarding.test.ts`: правило EXPOSED говорится в ход первого попадания по модулю,
      цена сгоревшего модуля — в ход первого выгорания, и каждая подсказка звучит ровно раз за
      рейс
- [x] **Три языка, EN · ES · RU** — `L` и `?lang=`, `tests/i18n.test.ts`: ни одной строки без
      перевода, ни панель, ни карточка, ни строка действия не вылезает за свою ширину ни в одном
      языке, забег на одном сиде одинаков во всех трёх
- [x] **Два вью, ASCII и веб** — `V` и `?view=web`, `tests/view.test.ts`,
      `tests/panel-html.test.ts`, `tests/schematic-svg.test.ts`: оба рисуют одни и те же чистые
      функции, вью не решает ничего
- [x] **Звук и музыка** — один трек владельца, громкость которого ведёт состояние забега, плюс
      стингеры и эффекты: `tests/music-state.test.ts`, `tests/sfx.test.ts`,
      `tests/audio-assets.test.ts`; выключается параметром `?sound=off` (`soundEnabled`,
      `src/ui/music.ts`). Авторство — `assets/CREDITS.md`, там же сказано, что трек сделан в Suno
- [x] **Вспышка на появление машины, в такт треку** — `tests/pulse.test.ts`: две доли, мигание по
      доле, без звука та же длительность по сетке, при `prefers-reduced-motion` цвет без мигания
      и без единого таймера
- [x] **Кривая сложности** — пороги и метрики в `tests/balance.test.ts` (числа смотреть там, не
      здесь) и `tests/winnable.test.ts`
- [ ] Забег доведён до победы руками минимум дважды — **владелец**
- [ ] Твист понятен в первые двадцать ходов без чтения текста — **владелец**: тесты меряют, что
      подсказка сказана, а не что она понята

## Соответствие джему

- [x] **Пройден `docs/traditional-checklist.md`** — построчный проход в статусе
      `docs/tasks/G13-release.md`. Одна строка не выполняется сознательно: *grid-based* —
      «нет: граф отсеков» (GDD, «Почему это traditional roguelike и run-based без меты»)
- [x] **Нет мета-прогрессии** — в `localStorage` лежат ровно два ключа, `salvor.lang` и
      `salvor.view`, оба настройки. `tests/purity.test.ts` запрещает `localStorage` в правилах
      игры вообще (`content/`, `systems/`, `twist/`, `rooms/`), а `tests/view.test.ts` держит
      контракт «настройка, никогда прогресс»
- [x] **Пошаговость** — мир двигается только через `RoomGame.playerCommand()`;
      `tests/purity.test.ts` не пускает в правила ни `Date.now`, ни `performance.now`, ни
      `Math.random`, то есть часов там нет физически
- [x] **Permadeath** — отката хода нет, сейва «продолжить позже» нет; погибший дрон, его рига и
      его лут не возвращаются, а корабль помнит смерть и поднимает призрака —
      `tests/persistence.test.ts` («a drone that died is still aboard»)
- [x] **PCG решает** — генерируется весь корабль: граф отсеков и дверей, замки и место ключа,
      состав машин, что и где лежит, какие корпуса и контракты предложены на точке рейса.
      `tests/derelicts.test.ts`
      («every hull, generated»), `tests/ship.test.ts`, `tests/winnable.test.ts`: на каждом классе
      и каждом сиде ровно по одному отсеку каждой системы, все три достижимы, и для корпуса без
      резака — без единой заваренной двери на пути
- [x] **Один персонаж** — в корабле всегда ровно один дрон

## Страница

- [x] Текст страницы написан и сверен с кодом — `docs/itch-page.md`: клавиши сверены с
      `src/ui/input.ts` и `tests/input.test.ts`, вьюпорт пересчитан от `src/ui/theme.ts`
- [x] `npm run zip` из корня собирает `games/salvor` и кладёт рядом `salvor-web.zip`
- [ ] 4 скриншота — **владелец**. Первый обязан показывать схему корабля, стойку модулей и
      список действий одновременно
- [ ] Гифка твиста, 5–8 с — **владелец**. Самый ценный элемент страницы
- [ ] Обложка 630×500 — **владелец**
- [ ] Размер вьюпорта в форме itch — **владелец**; число и как оно посчитано — в `docs/itch-page.md`
- [ ] Открыть в приватном окне без логина; Chrome, Firefox, Safari; ноутбучный 1366×768 без
      скролла — **владелец**
- [ ] Заявка отправлена **утром 15 сентября**, не в 16:59 — **владелец**
- [ ] После отправки: сыграть в 5 чужих заявок и оставить содержательные отзывы — **владелец**
